# Core ML and Deep Learning
torch>=1.9.0
torchvision>=0.10.0
numpy>=1.21.0
einops>=0.4.0

# GBST Tokenization for injection detection models
charformer-pytorch>=0.0.4

# Rule-based injection detection
# libinjection-python>=1.0.0  # Commented out - requires Visual C++ build tools on Windows
# Alternative: Use regex-based detection or install Visual C++ Build Tools

# Web Framework and API
flask>=2.0.0
flask-cors>=3.0.10

# Data processing and utilities
typing-extensions>=4.0.0
python-dotenv>=0.19.0

# Optional: Enhanced detection engines (if available in your system)
scikit-learn>=1.0.0
pandas>=1.3.0

# Development and testing (optional)
pytest>=6.0.0
pytest-cov>=3.0.0

# Production deployment (optional)
gunicorn>=20.0.0
# uwsgi>=2.0.20  # Commented out - causes issues on Windows

# Monitoring and logging (optional)
psutil>=5.8.0

# Network packet analysis for sniffer (if used)
scapy>=2.4.5

# Additional security libraries (optional)
cryptography>=3.4.0

# Database - SQLite support
sqlalchemy>=2.0.0  # ORM for database operations
flask-sqlalchemy>=3.0.0  # Flask extension for SQLAlchemy